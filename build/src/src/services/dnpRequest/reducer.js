// Service > dnpRequest

/**
 * @param state = dnps = {
 *   "bitcoin.dnp.dappnode.eth": {
 *     name: "bitcoin.dnp.dappnode.eth",
 *     manifest: {},
 *     avatar: {Base64Img}
 *     ...
 *   }, ... }
 */

export default (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
