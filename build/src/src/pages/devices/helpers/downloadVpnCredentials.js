/**
 * - Parses a DAppNode VPN's OpenVPN credentials URL
 * - Fetches the credentials file
 * - Downloads it
 * @param {string} url = http://origin/?id=<id>#<key>
 */
function downloadVpnCredentials({ id, url }) {}

export default downloadVpnCredentials;
